import random
servers = ["S1", "S2", "S3"]

def random_load_balance(request_id):
    server = random.choice(servers)
    print(f"Request {request_id} → {server}")
    return server

# Simulate requests
for i in range(1, 7):
    random_load_balance(i)